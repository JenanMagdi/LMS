// import { db } from "../database/Firebase";

// export const createLesson = (courseId, lessonTitle, content, videoLink) => {
//     return db.collection('courses').doc(courseId).collection('lessons').add({
//       title: lessonTitle,
//       content: content,
//       videoLink: videoLink
//     });
//   };
  